/* Dopo aver caricato un vettore confrontare la somma dei componenti pari
con la somma dei componenti dispari e dire se la somma maggiore è divisibile
            per il valore assegnato k (diverso da 0). */

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "array.h"
#include "array.c"
#define DIM 4

int main(){

    int v[DIM]={0}, k;

    riempivettore(v[], DIM);


 
}