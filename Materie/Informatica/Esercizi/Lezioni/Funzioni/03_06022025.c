/* Dato il raggio calcolare la circonferenza e l'area del cerchio. 
        Eseguire i passaggi dei parametri per indirizzo. 
        pi=3.14 c=2*pi*r a=pi*r^2 */

#include <stdio.h>

float circonferenza(*_c);
float area(*_a);

int main(){

    float r,c,a,pi=3.14;

    printf("Inserisci il valore del raggio: ");
    scanf("%f", &r);

}

float circonferenza(*_c){

    *_a
}